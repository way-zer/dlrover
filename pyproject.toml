[project]
name = "dlrover"
version = "0.5.0.dev0+ray_unified"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "clipstick>=0.6.1",
    "deprecated",
    "grpcio",
    "omegaconf",
    "packaging",
    "psutil",
    "pydantic",
    "pynvml",
    "ray[default,train]",
    "requests",
    "tornado",
    "urllib3",
]

[dependency-groups]
dev = [
    "coverage>=7.9.2",
    "deepspeed>=0.17.3",
    "kubernetes>=33.1.0",
    "pytest>=8.4.1",
    "pytest-async>=0.1.1",
    "pytest-forked>=1.6.0",
    "pytest-mock>=3.14.1",
    "transformers>=4.53.2",
    "verl>=0.3.0.post1",
    "vllm>=0.10.0",
]
torch = ["torch", "tensordict"]
